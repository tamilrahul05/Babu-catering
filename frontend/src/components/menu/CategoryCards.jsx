import React from 'react';
import { motion } from 'framer-motion';
import { Link } from 'react-router-dom';
import { ChevronRight } from 'lucide-react'; const CategoryCards = ({ categoriesConfig }) => { return ( <motion.div key="selection" initial={{ opacity: 0, scale: 0.98 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: 0.95 }} className="flex flex-col items-center justify-center min-h-[90vh] px-6 py-20 bg-zinc-950" > <div className="text-center mb-16 max-w-2xl"> <span className="text-amber-500 font-bold tracking-[0.3em] uppercase text-xs block mb-6">Personalized Dining</span> <h1 className="text-4xl md:text-7xl font-black mb-6 text-white">Choose Your <span className="text-transparent bg-clip-text bg-gradient-to-r from-amber-300 via-yellow-500 to-amber-600 italic">Style</span></h1> <p className="text-zinc-400 text-lg md:text-xl font-light tracking-wide">Select a category below to start building your custom catering plate.</p> </div> <div className="flex flex-wrap justify-center gap-10 max-w-7xl"> {categoriesConfig.map((cat, idx) => ( <Link to={`/menu/${cat.id}`} key={cat.id} className="w-full sm:w-[380px]"> <motion.div className="relative aspect-[3/4] rounded-[3rem] overflow-hidden group transition-all ring-1 ring-zinc-800" > {/* Image */} <img src={cat.img} alt={cat.title} className="w-full h-full object-cover transition-transform duration-1000 " /> <div className="absolute inset-0 bg-gradient-to-t from-black via-black/20 to-transparent"></div> {/* Clean Bottom Label with Glassmorphism */} <div className="absolute inset-x-6 bottom-6 p-8 bg-zinc-900/40 border border-white/10 rounded-[2rem] text-white transition-all duration-500"> <h2 className="text-3xl font-black mb-2">{cat.title}</h2> <div className="flex items-center gap-2 font-bold transition-transform"> View Menu <ChevronRight size={18} className="text-amber-500" /> </div> </div> </motion.div> </Link> ))} </div> </motion.div> );
}; export default CategoryCards;
